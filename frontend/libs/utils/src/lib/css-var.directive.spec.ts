import { CssVarDirective } from './css-var.directive';

describe('CssVarDirective', () => {
  it('should create an instance', () => {
    const directive = new CssVarDirective();
    expect(directive).toBeTruthy();
  });
});
